const CTAButton = ({text}: {text: string}) => {
    return (
        <button className="text-white px-16 py-4 font-serif whitespace-nowrap border-[0.5px] border-solid border-[#F4F7F5]">
            {text}
        </button>
    )
}

export default CTAButton
